---
apiVersion: v1
kind: Service
metadata:
  name: output
  labels:
    app: output
  namespace: stocks
spec:
  type: NodePort
  selector:
    app: output
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
